"""Project shared library (multi-phase).

This package contains reusable code shared across phases.
Phase 1 focuses on ETL (MongoDB -> PostgreSQL), but later phases can add more
modules while keeping imports and configuration consistent.
"""

