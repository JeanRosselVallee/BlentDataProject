#
# requirements.txt
#
# Purpose:
# - Minimal runtime dependencies for the ETL skeleton (Python 3.11+).
# - Used by local development and may be used by Render during builds.
#
# Note:
# - Versions are intentionally loose for now; pin later once your environment is stable.
#
pymongo>=4.7
#psycopg[binary]>=3.2
python-dotenv>=1.0
python-dateutil>=2.9
#tabulate>=0.10.0
pandas>=3.0.2
sqlalchemy>=2.0.49
psycopg2-binary>=2.9.12  # interface PostgreSQL-Python
